# 2026-04-03 日志

## 儿童汉字笔顺学习网页
- 纠纠要做儿童启蒙写字短视频，先做网页预览版
- 使用 hanzi-writer 库（CDN），田字格风格，逐笔顺动画
- 语音播报：每画播报笔画名 + 最后念出字音+拼音+总画数
- 支持任意汉字输入、重播、练习模式
- 文件：`C:\Users\caohongbo\WorkBuddy\Claw\hanzi-stroke-preview.html`
- 本地服务器：`server.js` 在 port 3456 提供服务

## 修复笔画顺序错误
- 纠纠反馈"四"和"五"的语音播报与实际笔顺不符
- "四"正确顺序：竖、横折、撇、竖弯、横
- "五"正确顺序：横、竖、横折、横
- 方案：创建 `STROKE_TABLE` 正确笔画表，覆盖常用字（40+ 字）
- 未知字用 `inferStrokeName` 几何推断（相对准确但不完美）
- 持续扩充 STROKE_TABLE 可提高准确性

## 2026-04-03 13:52 修复语音播报和练习模式失效
- **问题**：`getStrokeNameByIndex()` 函数缺失，导致：
  - 语音播报无法获取笔画名，只报"笔画"
  - 练习模式提示也是"笔画"，无法反馈正确笔画名
  - 最终完成时语音播报正常（因为只读字音+画数）
- **修复**：
  1. 添加 `getStrokeNameByIndex()` 函数，从 `strokeNames` 数组获取笔画名
  2. 修正语音播报时机：先报笔画名，同时播放动画（异步并行），而不是等语音播报完才开始动画
  3. 这样"一边语音播报一边写"的原始需求得以恢复
- **结果**：语音播报、动画播放、练习模式全部恢复正常

## 2026-04-03 14:45 准备 Vercel 部署
- **项目名称**：纠纠学字 (jiujiu-learn-words)
- **部署目标**：Vercel 免费托管，支持自动更新
- **配置文件**：
  - `vercel.json`：Vercel 部署配置
  - `index.html`：根路径重定向到主页面
  - `README.md`：项目说明文档
  - `DEPLOYMENT_GUIDE.md`：详细的部署教程（Git + GitHub + Vercel）
  - `.gitignore`：Git 忽略文件
- **优势**：后续只需推送代码到 GitHub，Vercel 自动部署新版本，无需手动操作


